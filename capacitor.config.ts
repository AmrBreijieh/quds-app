import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'مدرسة القدس : لتعليم السياقة',
  webDir: 'out',
};

export default config;
